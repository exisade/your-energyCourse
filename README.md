# Your Energy – Fitness Exercise Application

[![Vite](https://img.shields.io/badge/Vite-5.4.6-646CFF?logo=vite&logoColor=white)](https://vitejs.dev/)
[![JavaScript](https://img.shields.io/badge/JavaScript-ES6+-F7DF1E?logo=javascript&logoColor=black)](https://www.ecma-international.org/)
[![SCSS](https://img.shields.io/badge/SCSS-Sass-CC6699?logo=sass&logoColor=white)](https://sass-lang.com/)

---

## 📌 Загальний опис проєкту

**Your Energy** — це односторінковий веб-застосунок (SPA), призначений для перегляду, пошуку та підбору фізичних вправ на основі даних з backend API.

Проєкт реалізовано з використанням **Vanilla JavaScript**, без застосування будь-яких frontend-фреймворків (React, Vue, Angular), відповідно до вимог курсової роботи курсу **GoIT**.

Основна мета проєкту — надати користувачу зручний та інтуїтивно зрозумілий інтерфейс для роботи з каталогом вправ, фільтрацією, пошуком, рейтингами та списком обраних вправ.

---

## 📦 Встановлення

### Передумови
Переконайтеся, що у вас встановлено:
- Node.js (версія 14 або вище)
- npm або yarn

### Кроки встановлення

1. **Клонуйте репозиторій**
```bash
git clone https://github.com/your-username/js-curse-project.git
cd js-curse-project
```

2. **Встановіть залежності**
```bash
npm install
```

3. **Запустіть dev сервер**
```bash
npm run dev
```

Застосунок буде доступний за адресою: `http://localhost:5173`

## 📜 Доступні команди

| Команда | Опис |
|---------|------|
| `npm run dev` | Запуск dev сервера з hot reload |
| `npm run build` | Створення production build |
| `npm run preview` | Перегляд production build локально |

## 🏗️ Структура проєкту

```
js-curse-project/
├── src/
│   ├── css/                    # SCSS стилі
│   │   ├── common.scss         # Загальні стилі
│   │   ├── variables.scss      # Змінні (кольори, розміри)
│   │   ├── header.scss         # Стилі хедера
│   │   ├── footer.scss         # Стилі футера
│   │   ├── home.scss           # Стилі головної сторінки
│   │   ├── exercises.scss      # Стилі каталогу вправ
│   │   ├── exercise-modal.scss # Стилі модалки вправи
│   │   ├── rating-modal.scss   # Стилі модалки рейтингу
│   │   └── fonts.scss          # Шрифти
│   │
│   ├── js/                     # JavaScript модулі
│   │   ├── exercises.js        # Каталог вправ та фільтри
│   │   ├── exercise-modal.js   # Модальне вікно вправи
│   │   ├── rating-modal.js     # Модальне вікно рейтингу
│   │   ├── favorites.js        # Робота з обраним
│   │   ├── header.js           # Функціонал хедера
│   │   └── quote.js            # Цитата дня
│   │
│   ├── partials/               # HTML компоненти
│   │   ├── header.html         # Хедер
│   │   ├── footer.html         # Футер
│   │   ├── home.html           # Головна секція
│   │   ├── exercises.html      # Каталог вправ
│   │   ├── exercise-modal.html # Модалка вправи
│   │   └── rating-modal.html   # Модалка рейтингу
│   │
│   ├── img/                    # Зображення
│   │   └── svg/                # SVG іконки
│   │
│   ├── fonts/                  # Шрифти
│   │   ├── DMSans.ttf
│   │   └── DMSans-Italic.ttf
│   │
│   ├── index.html              # Головний HTML файл
│   └── main.js                 # Точка входу JavaScript
│
├── dist/                       # Production build (генерується)
├── vite.config.js              # Конфігурація Vite
├── package.json                # Залежності та скрипти
└── README.md                   # Документація

```

## 🔌 API Integration

Застосунок використовує API від GoIT:

**Base URL:** `https://your-energy.b.goit.study/api`

### Endpoints

| Method | Endpoint | Опис |
|--------|----------|------|
| `GET` | `/filters` | Отримання фільтрів (категорій) |
| `GET` | `/exercises` | Отримання списку вправ |
| `GET` | `/exercises/:id` | Отримання деталей вправи |
| `POST` | `/exercises/:id/rating` | Додавання рейтингу |
| `GET` | `/quote` | Отримання цитати дня |
| `POST` | `/subscription` | Підписка на розсилку |

### Приклад запиту

```javascript
// Отримання вправ за фільтром
const response = await fetch(
  'https://your-energy.b.goit.study/api/exercises?muscles=biceps&page=1&limit=10'
);
const data = await response.json();
```

## 🎨 Особливості реалізації

### Event Delegation (Оптимізація)

Замість накладання окремих слухачів на кожну картку вправи, використовується делегування подій:

```javascript

cardsContainer.addEventListener('click', event => {
  const card = event.target.closest('.exercises__content__main__cards-item');
  
  if (!card) return;
  
  
  const categoryName = card.getAttribute('data-category-name');
  const exerciseId = card.getAttribute('data-exercise-id');
  
  
});
```

**Переваги:**
- ⚡ Один слухач замість N (де N може бути 100+)
- 💾 Менше споживання пам'яті
- 🚀 Швидший рендеринг карток
- 🔄 Автоматична робота з динамічним контентом

### LocalStorage для Обраного

```javascript

const favorites = JSON.parse(localStorage.getItem('favorites') || '[]');
favorites.push(exerciseId);
localStorage.setItem('favorites', JSON.stringify(favorites));
```

### Адаптивний дизайн

- Mobile First підхід
- Breakpoints: 375px, 768px, 1440px
- Flexible Grid система

## 🎯 Функціональні можливості застосунку

### 🏋️ 1. Каталог вправ
- Отримання списку вправ з backend API
- Відображення вправ у вигляді карток
- Фільтрація вправ за категоріями:
  - **Muscles**
  - **Body parts**
  - **Equipment**
- Підтримка серверної пагінації
- Динамічне оновлення контенту без перезавантаження сторінки

---

### 🔍 2. Пошук вправ
- Пошук за ключовими словами
- Пошук виконується за назвою вправи
- Пошук працює в межах активної категорії
- Запити оптимізовані для зменшення навантаження на API

---

### ⭐ 3. Обране (Favorites)
- Додавання вправ до списку улюблених
- Збереження списку у `localStorage`
- Відображення обраних вправ на окремій сторінці
- Можливість видалення вправ з обраного
- Синхронізація UI зі станом `localStorage`

---

### 💬 4. Система рейтингу
- Відображення рейтингу вправ
- Можливість залишити оцінку вправи
- Валідація email перед надсиланням
- Відправка рейтингу на backend
- Обробка серверних помилок та повідомлень

---

### 📄 5. Пагінація
- Серверна пагінація
- Перемикання сторінок без перезавантаження
- Збереження активної сторінки під час навігації

---

### 💡 6. Цитата дня
- Отримання цитати з backend API
- Збереження цитати та дати в `localStorage`
- Повторне використання цитати без додаткового запиту протягом дня

---

### 📱 7. Адаптивний дизайн
- Mobile-first підхід
- Коректне відображення на:
  - мобільних пристроях
  - планшетах
  - десктопах
- Адаптивна сітка та гнучкі блоки

---
## 🔧 Налаштування

### Зміна Base Path

У файлі `vite.config.js` змініть параметр `base`:

```javascript
base: command === 'serve' ? '/' : '/your-repo-name/',
```

### Додавання нових API endpoints

У файлі `src/js/exercises.js` або інших JS модулях:

```javascript
const API_BASE = 'https://your-energy.b.goit.study/api';

async function fetchData(endpoint) {
  const response = await fetch(`${API_BASE}${endpoint}`);
  return await response.json();
}
```

## 📱 Підтримка браузерів

- Chrome (остання версія)
- Firefox (остання версія)
- Safari (остання версія)
- Edge (остання версія)
- Mobile Safari (iOS 12+)
- Chrome Mobile (Android 8+)

## 📝 Ліцензія

ISC License

## 👨‍💻 Автор

Toma Denys - [reewd22@gmail.com](mailto:reewd22@gmail.com)





